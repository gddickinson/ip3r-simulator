# INTERFACE.md — navigation map

Read this before opening any source file. Every `.py` is under 500 lines.

## Dependency direction

```
io ──▶ core ──▶ structure ──▶ physics ──▶ analysis
                                  │
                   render ◀───────┴───────▶ ui
```

`physics` and below never import `render` or `ui`, so the science runs
headless (CLI, tests, notebooks).

## Repository layout

| Path | Contents |
|---|---|
| `ip3r/` | the application package |
| `scripts/` | maintenance: parameter build, ip3r_genes import, GUI smoke test |
| `tests/` | pytest suite; real-data tests skip when data is absent |
| `docs/SCIENCE.md` | the models, their equations and sources; what each check establishes |
| `docs/SCIENCE_CHECKS.md` | the findings checks paper by paper: how each re-derives its numbers (modules, shells and their FEL rates, the tree and its `--bnni` re-search, the genome grid, presence/absence, the family-call benchmark, VUS strata) and what agreement establishes |
| `docs/SCIENCE_PUFF_DOMAIN.md` | the park/drive cluster in Cao's microdomain with fluo-4: IPIs vs h42 recovery, amplitude vs N, and the sustained-open question under five clamps |
| `docs/SCIENCE_PERM.md` | the pore permeation model: K⁺ conductance of each state, the shortfall in 3-D (Round 7.6), wall charge, salt bridges, selectivity / i_Ca under Vais 2010's protocols, and the lining groups' protonation (with RyR1 as the control) |
| `docs/SCIENCE_RYR.md` | the same for the ryanodine receptor: structures, conductance, mutants, gating, sparks (mean-field and in the cleft) |
| `docs/SCIENCE_EC.md` | the couplon under voltage clamp: V channels, Stern's calibration, SR depletion |
| `docs/img/` | screenshots written by `scripts/screenshot_app.py` |
| `ROADMAP.md` | what is not done, in rounds (Round 6, the ryanodine receptors, in `ROADMAP_RYR.md`) |
| `SESSION_LOG.md` | what was done and why, per session |
| `Makefile` | every task (`make help`) |
| `run_app.command` | launcher: activates `ip3r_sim` and runs `python -m ip3r` (double-clickable on macOS; arguments pass through) |
| `ref/`, `data/` | downloads and derived output — git-ignored, regenerable |

## `ip3r/` — top level

| File | Purpose |
|---|---|
| `config.py` | paths (`RESOURCE_DIR`, `REF_DIR`, `GENES_DIR` = `../ip3r_genes` or `$IP3R_GENES_DIR`, `genes_results()` resolved at call time), `PARALOG_ACC` / `PARALOGS` (the publication's three), `RYR_ACC` (rabbit RyR1 P11716), `NUMBERINGS` (both: what a deposit can be numbered in), `DEFAULT_STRUCTURE` (6DQN), `RenderSettings` |
| `parameters.py` | the parameter registry (`PARAMETERS.value(key)`, overrides tracked, `IP3R_PARAMETERS` override file; `subscribe` for change listeners — caches of parameter-dependent results and the GUI banner; `matches` (editor filter), `write_overrides`/`read_overrides`, `replace` (whole set, one notification); `references()` for tooltips). Ported from PIEZO1. |
| `cli.py` | `python -m ip3r <fetch|info|checks|states|unitary|modes|transition [--cutoff-scan --stride-check]|gating [--model dyk\|mak\|pd]|oscillate|puffs|params>`; no argument launches the GUI |
| `cli_graft.py` | `register(sub)`: `graft PDB [--mode full] [--calibrate] [--long]` (fill summary and per-stretch rows; `--calibrate` = stretches other deposits miss; `--long` = windows 10–60 by length and islands) |
| `cli_shortfall.py` | `register(sub)`: `shortfall [PDB] [--scan]` (every open deposit: 1-D, lumen-area 1-D, 3-D and the favourable corner vs measured; `--scan` = window, seal and grid on PDB) |
| `cli_domain.py` | `register(sub)`: `microdomain [--ip3 --n --clamp store\|none\|bath --scale] [--scan ah42\|n\|store]` |
| `cli_perm.py` | `register(sub)`: `selectivity [PDB] [--closure donnan\|radial]` (Vais 2010's P_Cl:P_K, P_Ca:P_K, i_Ca on every wall reading); `protonation [PDB] [--corners] [--no-propka]` (lining pKas by both routes, selectivity under each; RyR1 adds Xu's mutants) |
| `cli_ryr.py` | the RyR1 commands, `register(sub)`: `mutants`, `ryr-gating`, `sparks [--scan] [--cleft [--fit]]`, `spark-termination [--scan fit\|ki\|rate\|use\|ratio\|fraction\|low-activity\|speed] [--bell] [--fitted]`, `spark-mg [--scan] [--reading R] [--spontaneous] [--two-site]`, `ec [--scan] [--reading R] [--only S] [--trials N] [--two-site] [--use] [--depletion [--pool-scan] [--large]]` |
| `__main__.py` | entry point |

## `ip3r/io/`

| File | Key names |
|---|---|
| `cif_reader.py` | `read_structure_file()` — fast mmCIF/PDB → numpy arrays (ported) |
| `registry.py` | `StructureEntry` (`family`: IP3R / RyR), `load_registry()`, `get_entry()`, `local_path()` — the 9 IP3R depositions from `resources/structures.json` plus the 6 RyR1 ones from `resources/ryr1.json` and the open-state control 7T3T from `resources/ip3r_controls.json` (`is_control`: role `open_control`, left out of the state panel) |
| `fetch.py` | `fetch_structure()`, `fetch_all()`, `is_valid()` — RCSB `.cif.gz` into `ref/structures`; `IP3R_STRUCTURE_MIRROR` copies from a local mirror (e.g. the ip3r_genes data root) |
| `loader.py` | `load(pdb_id)` memoised; `ALLOW_FETCH` switch (off by default; the GUI and `--fetch` turn it on); `StructureUnavailable` |
| `predictions.py` | AlphaFold DB models into `ref/alphafold` (`fetch_prediction` discovers entry + version from the API; `ACCESSIONS`, `UNAVAILABLE` with why), `local_predictions`, `load_prediction` (memoised; pLDDT in `b_factor`) |
| `poly_seq.py` | `construct_sequences(path)` / `construct_sequence(path, strand)`: the construct from `_pdbx_poly_seq_scheme`, author number → residue name, unbuilt residues included (memoised) |
| `session.py` | `Session` (the view: deposit, style, colour, layer, subunits, sites, pore, completeness, camera, tab, transition spec, the panel views `PANEL_VIEWS` — `dynamics`, `modes` {index, amplitude}, `variants`, flat name → scalar dicts the panels write — and the parameter overrides it was saved under — never coordinates or results), `from_dict` refuses a wrong type or newer format by name, `save_session`/`load_session`, `parameter_differences` |

## `ip3r/core/`

| File | Key names |
|---|---|
| `structure.py` | `Structure` — structure-of-arrays container, masks, residue index (ported) |
| `annotations.py` | per paralog (or `RYR1`, `is_ryr`: sequence and domains from `ryr1.json`; no constraint/sites/variants, so grey): `reference_sequence`, `elements` (Pfam + 6DQN structural elements), `residue_elements` (one element per residue, specific wins), `element_of/element_array`, `functional_sites` (10 IP3 contacts, filter/gate lining), `residue_constraint/constraint_at` (S17 JSD, 4 layers), `variants`; colour/label tables |
| `modules.py` | Paper 6's modules rebuilt from sites + domains: `module(paralog, definition)` (`contact_span`, `ibc_literature` = Bosanac 2002's ITPR1 224–604 via `ligand.ibc_*`, carried by `pairwise`; `channel_minus_luminal`, `channel_all`; `CORE_DEFINITIONS`, `PORE_DEFINITIONS`) → `Module`, `modules()` (the primary pair, disjoint), `ModuleRefusal`, `MODULE_COLORS`, `MODULES_KEY` (the GUI's site toggle) |
| `pairwise.py` | `align` (Gotoh affine-gap global, BLOSUM62, end gaps free; gap costs `align.*`), `transfer_map`, `paralog_transfer(src, dst)` (memoised) — carries residue numbers between paralogs independently of S17's MAFFT |
| `genes_data.py` | live read-only access to `ip3r_genes/results`: `read_tsv`, `read_json`, `read_text`, `available`; raises `GenesDataMissing` (→ check `not_run`) |

## `ip3r/structure/` — measurement

| File | Key names |
|---|---|
| `symmetry.py` | `kabsch`, `rotation_matrix`, `rotation_axis_angle`, `axis_by_superposition` (our method), `axis_by_centroids` (S0's), `tetramer_frame` → `Frame` (z on the axis, cytosol +z, chains right-handed), `c4_residual` |
| `pore.py` | `pore_profile` (`r_min` = S0's quantity; `r_free` = less vdW; `radial_profile` on atoms already in frame), `tm_span`, `find_constrictions` (filter = luminal min, gate = cytosolic min; the rule itself is `constriction_indices`, S0's windows `PROFILE_MARGIN`/`GATE_MARGIN`), `lining_residues`; `include_hetero` reproduces S0 |
| `ligand.py` | `ligand_sites` (IP3 copies, subunit by proximity), `neighbourhood` (atoms within the shell radius: what the site view frames), `contacts` (≤ cutoff, own vs other subunit), `residue_distances` (S22 shells; `heavy_only=False` = S22's all-atom rule) |
| `shells.py` | S22's ligand shells: `shell_edges`/`shell_of` (registered edges, half-open), `chain_distances` (own-subunit IP3, all atoms), `deposit_distances`, `consensus_shells` → `ShellResidue` (median over deposits), `atom_ligand_distance` (per atom, for painting) |
| `numbering.py` | `check_numbering`, `best_numbering` — S24's rule, stubbed (backbone+CB) residues excluded, mismatch segments reported |
| `channel.py` | `measure_channel(st)` → `ChannelSummary` (axis two ways, residual, numbering, span, profile, constrictions, IP3 contacts) — shared by GUI, CLI and checks |
| `transition.py` | `prepare_transition(start, end, fit)` → `Transition` (`meta["end_transform"]` = the end's superposition;residue-matched basis: unstubbed, sequence-matching, all 8 chains; cyclic subunit correspondence; end superposed onto the start *as deposited*; `residue_distance`, `element_means`), `atom_site_index` (own residue, else nearest site in space), `displaced_coords`, `atom_displacement` (NaN off basis), `TransitionUnavailable` |
| `morph_pore.py` | the gate along a morph: `atom_path(start, end, tr)` → `AtomPath` (heavy atoms matched by name; Cα offset interpolated, `coords`, `rigid` = the old shortcut, `offset_error` chord, `index` into the start deposit), `gate_path(path, tr, mt)` → `GatePath` (gate/filter per frame, `rigid_gate`, lining, `half_open`, `overshoot`); axis re-found per frame |
| `morph.py` | `morph(start, end, method)` → `MorphTrajectory` (`restrained` / `linear`, `bond_error`, `nearest`), `peptide_pairs`, `NOTE` (the "interpolation, not trajectory" sentence) |
| `graft.py` | AlphaFold fills: `prediction_for(st)` → `(model, NumberMap)` (by number, else by alignment of the construct, else `GraftRefusal` with both identities), `unresolved` → `Stretch` (gap / n_term / c_term), `fill_stretches(numbering=)` (a stretch touching an unpaired segment or an insertion skipped by name; fill atoms in deposit numbers), `fill_structure(st, mode)` → `FilledModel` (predicted atoms only; `numbering`; `very_low()` = residues below `display.plddt_low`; `Fill` per stretch: anchor RMSD, seam distances, pLDDT, clashes; `Skip` with reason; `place(xyz)` re-fits to new coordinates; `seam_segments`), `FILL_MODES`, `placed_ca` |
| `graft_numbering.py` | `NumberMap` (`route` number / alignment, `identity`, callable deposit → prediction residue, `unmapped` segments, `lacking`, `span`, `describe`), `BY_NUMBER`, `by_number`, `by_alignment(construct, pred_res)` (`core.pairwise`; identity over pairs, `graft.align_min_identity`), `construct_of(st, chain)`, `runs` |
| `graft_calibration.py` | `calibrate(host, others, pred)` → `Trial`s: stretches others miss, hidden in the host, filled, scored vs a straight line and a global fit (`plddt_scored` = the compared residues'); `candidate_stretches`, `hide`, `trial(span=)`, `global_fit`; Round 7.9: `windows` / `window_trials` (resolved stretches of a given length), `islands` / `island_trials` (a resolved run between two gaps hidden, the whole span filled, scored on the island) |
| `states.py` | `state_panel(paralog)` → `StateRow`s: every human deposit measured the same way (the gating transition at the pore); controls excluded |
| `pore_volume.py` | the ion-accessible lumen as voxels (Round 7.6): `open_voxels` (centre clears vdW + probe, hard spheres), `volume_from_mask` (membrane = span sealed beyond `pore3d.seal_radius`, bath faces, the component joining both baths), `volume_from_atoms`, `pore_volume(st, frame, span, probe)` → `PoreVolume` (`mask`, `top`/`bottom`, `slice_area` = the in-plane region around the axis) |

## `ip3r/physics/` — simulation

| File | Key names |
|---|---|
| `anm.py` | `build_hessian` (inverse-square springs), `ANM.calc_modes` (drops 6 × components), `ANM.label_symmetry` (C4 irreps A/B/E), `apply_generator`, `ModeSet` (`collectivity` κ, `is_collective`, `first(irrep)` skips local artefacts), `tetramer_sites`, `atom_displacements` |
| `network_checks.py` | the network against its own choices: `local_modes` → `LocalMode` (residue a low-κ mode sits on, the unresolved stretch between its sampled neighbours), `describe_local`, `cutoff_scan` → `CutoffRow` (lowest collective A, collective A together, cumulative, per cutoff of the registered grid `cutoff_grid`), `rmsip`, `stride_agreement` (RMSIP vs stride 1) |
| `transition_modes.py` | `transition_overlap(tr, reference, cutoff=)` → `TransitionOverlap` (overlap, cumulative, symmetry-matched null, irrep fractions, `subspace(irrep)` → `Subspace` (one irrep's collective modes together, lowest first: cumulative, null, √fraction ceiling), `report()`), `remove_rigid_body`, `irrep_fractions`, `null_cumulative` |
| `gating.py` | De Young–Keizer / Li–Rinzel: `m_inf`, `n_inf`, `q2`, `h_inf`, `tau_h`, `open_probability`, `bell_peak`, `hill_fit_left_flank`, `bell_at` (via `bell`), `GatingParams` |
| `gating_mak.py` | Mak et al. 1998 Hill-type steady state: `MakParams`, `k_inh` (Eq. 2), `open_probability` (Eq. 1), `bell_at`, `compare_flanks` (DYK, Mak and park/drive, one ruler) |
| `bell.py` | model-agnostic bell measurement: `measure_bell(f)` → `Bell` (peak, half-activation, half-inhibition, `width_decades`), `flank_shifts` |
| `calcium.py` | closed-cell Li–Rinzel: `simulate` → `Trace`, `fluxes`, `oscillation_metrics` (sustained only), `oscillation_window` (0.36–0.63 µM measured), `steady_state`, `CellParams` |
| `puffs.py` | stochastic DYK cluster: `simulate_cluster` → `PuffTrace` (`n_open` snapshot per `puff.record_dt` bin, `n_peak` most open within it, `peaks`), `detect_events` (on peaks), `fano`, `PuffParams` |
| `park_drive.py` | Siekmann/Cao park/drive receptor: `ParkDriveParams` (the 41 `pd.*` constants), `ip3_functions`, `gate_inf`, `mode_rates`, `constant_generator`, `stationary` (detailed balance), `open_probability`, `park_fraction`, `drive_open_probability`, `bell_at`; `STATES`/`OPEN`/`PARK` |
| `puffs_pd.py` | park/drive cluster: `ReceptorCluster` (n receptors, one step at a time given the Ca²⁺ a closed and an open receptor see: exact `expm` within modes, then mode switch; shared with `puffs_domain`), `simulate_cluster_pd` (mean field; gate equilibria tabulated per number open; `clamp_ca` for the single-channel condition), `ParkDrivePuffParams` |
| `microdomain.py` | Cao 2014's pools with fluo-4 in the microdomain (Cao 2013 Eq. 12): `DomainParams` (`scaled` = k_ipr multiple), `start_store` (the code's 449 µM), `rest_state` (cytosol steady against it), `whole_cell_rest`, `CLAMPS` (none / store / bath) + `clamped`, `rhs`, `rk4_step`, `store`, `fluorescence`, `coupling_per_open` |
| `puffs_domain.py` | `simulate_cluster_domain` → `DomainTrace` (a `PuffTrace` with `ca` = microdomain, `cytosol`, `store`, `f_ratio`/`f_peak` = F/F0, `rest`), `DomainPuffParams` (`clamp`) |
| `puff_stats.py` | puffs from fluorescence: `blip_df`, `fluorescence_events`, `puff_stats` → `PuffStats` (blips, puffs, amplitudes in dF/F0 / µM / open, IPIs, `rate`, `cv`, Thurley `lam`/`xi`, `refractory_lr`), `thurley_pdf`, `thurley_sample`, `fit_thurley` (MLE), `refractory_lr` |
| `puff_domain_scans.py` | `ah42_scan`, `n_scan(scale=)`, `store_scan` → `StoreRow`s over `CONDITIONS` (mean-field, deep mouth, bath, store held, store free) with `Occupancy` (open, late, active, Fano); `run_all` (one process per point; parameter sets built in the parent) |
| `permeation.py` | 1-D drift-diffusion (ported from PIEZO1): `IonSpecies`, `potassium_species` (symmetric KCl; sweep overrides), `solve_pnp(z, r_free, fixed_charge=, closure=)` → `PermeationResult` (ohmic closure uncharged, local electroneutrality charged or with different baths, each mouth Donnan-equilibrated with its own bath (TMS); `closure="radial"` uses `radial_pb` per species instead of one Donnan potential, `CLOSURES`; Hall access), `series_conductance` (closed-form check), `debye_length`, `blocking_mechanisms` (steric only) |
| `radial_pb.py` | the radial closure: `radial_partition(valences, reservoir, fixed, radius)` → `RadialPartition` (cylindrical Poisson–Boltzmann in every slice, Gauss at the wall, all slices in one banded Newton solve; `offset` = each species' cross-section partition as a potential, `axis`, `wall`), `screening_ratio` (R/λ_D), `EPS0` |
| `_pnp_kernels.py` | the discretisation, ported unchanged: Scharfetter–Gummel `_nernst_planck`, `_ohmic_potential`, `_donnan_potential`, `_neutrality_step`, row-scaled Dirichlet solve |
| `selectivity.py` | Vais 2010 through the pore: `ions(lumen, cytosol)` (asymmetric baths; impermeant NMDG⁺ left out), `ghk_ratio` (their Eq. 1) / `ghk_reversal` (its inverse), `reversal_potential` (root of the pore current), `selectivity` → `Selectivity` (P_Cl:P_K, P_Ca:P_K), `ryr1_calcium_ratio` (Xu 2006's protocol and Eq. 1: GHK with no Cl⁻ term), `calcium_current` → `CalciumCurrent` (i_Ca at 0 mV, slope), `ghk_calcium_permeability` (Vais's GHK estimate), `selectivity_panel(st, closure=)` → `Reading`s (neutral / charged / paired / acidic only), `slow_nmdg_bound`, `published` |
| `pore_charge.py` | wall charge from the deposit's side-chain atoms: `charged_groups` (charge centre within `pore_charge.lining_margin` of the lumen; stubbed residues → `unplaced`), `map_charge` (Gaussian, charge-conserving), `pore_charge(pair_bridges=, neutralise=, charges=)` → `PoreCharge` (`bridged`: the pairs dropped; `charges` = a mean charge per `(chain, residue)`, a protonation reading) |
| `pka.py` | the Tanford–Kirkwood network: `sites(st, around)` → `Site`s (Asp/Glu/His/Lys/Arg within `pka.site_radius`; stubs counted), `interactions` (W_ij in kT: vacuum Bjerrum / (Mehler–Solmajer `eps_sigmoidal` · r), Debye-screened; `eps=` uniform for the bound), `exact` (2^n enumeration, n ≤ 20), `monte_carlo` (single, pair and ring heat-bath moves; conditional-probability estimator), `ring_blocks`, `titrate` → `Titration` (`charge`, `apparent_pka`, `charges`, `by_residue`) |
| `pka_propka.py` | the second route: `context_mask` (whole residues within `pka.propka_radius`), `pdb_text`, `propka_pka` (PROPKA 3, side chains only; termini told apart by class), `charge_at` (Henderson–Hasselbalch) |
| `protonation.py` | Round 7.4: `lining_wall(st)` → `Wall` (profile, formal groups, `centres`, `density(charges, neutralise)`), `family_conditions` (Vais pH 7.3 / Xu pH 7.4 and the bath), `network_charges(wall, eps)`, `propka_charges`, `measure` → `WallReading` (P_Ca:P_K by the family's protocol, P_Cl:P_K for IP3R, g), `readings` (formal / network / ε 10 / ε 4 / PROPKA; `EPS_BOUNDS`), `corners` (every ring formal or neutral: the pKa-free bound), `interior` (random fractional states inside it) |
| `ryr_gating.py` | RyR1 by cytosolic Ca²⁺: `SternParams` (Stern 1997 two gates in series, `k_a`, `k_i`), `generator`, `stationary` (null space), `open_probability` (closed form), `bell_at`, `with_constants` (Ka, Ki, rate scale), `fit_to_bell` (off rates moved to both measured flanks); Mg²⁺ (`mg`, `k_mg_a` competitive at activation, `mg_i` equal to Ca²⁺ at inactivation; `k_a_eff`, `with_mg`); a scheme carries its own tables (`dest`, `open_mask`, `inact_mask`, `trigger_map`) and `exit_rates`/`generator`/`open_probability`, so every simulator runs any scheme; `MurayamaParams` (`at_37`), `murayama_activity` (Murayama 2015 Eq. 1), `murayama_bell`, `compare_bells`; `STATES`, `OPEN` |
| `ryr_use.py` | Laver & Lamb 1998's use-dependent (flux-driven) inactivation: `UseParams` (a `SternParams` plus a third gate entered **only from the conducting state**, so `s = a + 2i + 4u`, eight states, no detailed balance — `cycle_flux`; `available` = the ceiling it leaves, `tau_use`), `with_use`, `no_ca_gate` (the Ki-infinite control), `open_probability_no_ca_gate` (hand solution the tests hold the null space to), `fit_with_use` (Ka and Ki fitted *beside* the gate, so the measured bell is not counted twice; multi-start, raises only when no start lands), `with_use(ratio=)` (recovery = ρ × entry unless given), `bell_panel` → `BellRow` (over ρ), `tau_values`, `ratio_values`; Laver & Lamb's own numbers: `use_rate_at(v)` (Fig. 4's line), `residual_bound(R)` (Fig. 8's residual → ρ ≤ R/(1−R)), `decline_bound` (the most a use gate can lower activity in t at v, any ρ; Sitsapesan 1995's −40 mV check) |
| `ryr_mixed.py` | a mixed cluster (Round 6.11): `MixedUseParams` (a `UseParams` with `fraction` = `ryr.use_inactivating_fraction` and a per-channel `mask`; `for_cluster(n, rng)` binds, `exit_rates` refuses unbound, `draw` = each population's stationary state, `open_probability` = the population mean f·Po_use + (1−f)·Po_plain), `mixed(sp, fraction)`, `bind` (every simulator calls it once per run), `fit_mixed` (the shared Ca²⁺ gate fitted to the population bell, via `fit_with_use(fraction=)`; `low_activity_reading=` adds Copello's channels), `fraction_values`; `low_activity(c, reading)` + `LA_READINGS` (Copello 1997's low-activity population as a Hill pair, half points at the low/mid/high ends of the measured ranges) |
| `ryr_two_site.py` | Stern's C channel with a two-site inactivation gate (inactivated only with both Ca²⁺ bound; six states, `s = a + 2n`): `TwoSiteParams` (a `SternParams`: `k_inact2_off`, `k_i2`, `uninactivated`), `two_site(k_a, k1, k2)`, `fit_two_site` (both half-peak points and the bell's log-log slope at half inhibition), `bell_slope`, `gate_hill_slope` |
| `sparks.py` | RyR1 cluster: `SparkParams`, `diffusion_coupling` (point source at the channel spacing), `simulate_sparks` (exact step tabulated per number open; returns a `PuffTrace` with `n_inactivated`), `spark_couplings(base=)` |
| `cleft.py` | Stern 1997's junctional cleft: `CleftGeometry` (`from_parameters`), `couplon` (two rows, V/C chessboard; `c_sites`, `v_sites`), `v_coupling_matrix` (µM at each C channel per open V channel), `edge_transfer` (Eq. 13), `coupling_matrix` (finite-volume steady solve, µM at each C channel per open C channel; memoised per geometry), `field` (the cleft map), `nearest_coupling` |
| `sparks_cleft.py` | RyR1 array in the cleft: `CleftSparkParams` (`ca_per_open` = nearest-neighbour coupling), `couplings_for` (off-diagonal scaled, own release kept), `c_rates` and `initial_states` (shared with the couplon; a bound mixed scheme draws per population), `simulate_sparks_cleft` (Gillespie, exact; a `PuffTrace`; `trigger` opens the available channels at t = 0), `native_coupling`, `DEST` |
| `spark_termination.py` | what ends a cleft spark: `Termination` (sparks, median duration, `unterminated`, inactivated at end, open fraction), `measure(sp)`, `refit` (Stern vs fitted to Murayama 25/37 °C), `ki_scan`, `rate_scan(base)` (Ki fixed, rates scaled), `use_scan` (the use gate's speed at the registered recovery ratio, Ca²⁺ gate refitted at each), `ratio_scan` (its recovery/inactivation ratio ρ, the round's load-bearing constant), `no_inactivation` (neither gate: the induction-decay control), `fraction_scan` (share carrying the use gate, Ca²⁺ gate refitted to the population bell), `fraction_controls` (the two halves of a fraction below 1, apart), `low_activity_scan` (Copello's channels in the bell, each reading) |
| `use_speed.py` | Round 6.13, the use gate at Rios & Pizarro 2026's millisecond rates: `rios_rates` (ln2 over `ryr.rios_i1_half_time`/`ryr.rios_r1_half_time`), `rios_ratio`, `speed_values` (bilayer rate → Rios's), `SpeedRow` + `read` (one scheme, spontaneous and triggered), `speed_panel` (Ca²⁺ gate refitted at each speed), `controls` (Ca²⁺ gate removed; Rios alone), `fraction_panel` (Round 6.11's mixed cluster at both speeds) |
| `spark_mg.py` | Mg²⁺ and the cleft spark: `triggered(sp)` → `Triggered` (available channels opened at t = 0, timed to all shut; opened, ended, inactivated before/after), `dissect` (no Mg²⁺ / activation site / inactivation site / both at `ryr.mg_free`), `mg_scan`, `spontaneous`, `k_mg_a_by_ratio` (Laver 2004's selectivity on the fitted Ka), `READINGS` + `k_mg_a_reading` (the three K_Mg,A readings: measured, selectivity, meissner; shared by CLI and GUI) |
| `allosteric_v.py` | the V channel, Rios 1993's allosteric model: `RiosParams` (fiber 827; `rate_scale` = Stern's ×2), `generator(V)` (C0–C4, O0–O4), `exits` (the table the couplon draws from), `stationary`, `open_probability` (Eq. 6), `charge` (Eq. 7), `step_response` (master equation, Stern's Fig. 11 check) |
| `couplon.py` | the whole couplon: `CouplonParams` (C array + V model + V current), `step_protocol`, `v_trajectory` (one V channel through a voltage protocol, exact), `simulate_couplon` (V channels first, then the C array by Gillespie with V openings as scheduled events; `scale=(times, fractions)` makes every unitary current a piecewise-constant fraction, the SR content) → `CouplonTrace` (`flux()`, `scale`) |
| `lumen.py` | SR depletion (Stern's Fig. 20 pool): `LumenParams`, `mm_per_pa_ms` (couplon density → mM per pA·ms), `content_path` (release out, first-order uptake), `depleted_ensemble` → `Depleted` (path consistent with the ensemble's own release by damped fixed-point iteration; `released`, `peak_to_plateau`, `corrected` = Schneider's correction, `corrected_peak_to_plateau`, `full` = same couplons never depleting), `fig20_couplon` (28 channels), `depletion_panel`, `pool_scan` (pool × factor at Rios's +20 mV); `po_peak_to_plateau` |
| `ec_release.py` | release under voltage clamp: `ensemble(v, scale=)` → `Ensemble`, `summarise` → `Summary` (peak, plateau, after repolarisation, flux peak/plateau), `configurations(reading, two_site=, use=)` (Stern / fitted / fitted + Mg²⁺ at the activation site / both sites; `two_site` uses `fit_two_site`, `use` the bell-permitted use gates), `with_gating`, `events`, `event_stats` → `EventStats` (single C events at `ec.spark_voltage`), `voltages` |
| `mg_competition.py` | Meissner 1997's Eq. 4 with two competitors at the activation site: `hill_ka` (one competitor), `ka_shift(mg, na=)` (Mg²⁺'s shift of half-activation in Na⁺; default Murayama's 0.17 M), `equivalent_k_mg_a(mg=)` (the K_Mg,A giving that shift in this scheme's form; 769 µM at 1 mM) |
| `ryr_mutants.py` | RyR1 charge mutants vs Xu 2006: `mutants()` (from the registered `permeation.published_ryr1_*`), `mutant_panel(st)` → wild type + `MutantRow`s (measured vs modelled ratio, charged and paired; lining, bridged), `open_deposit`, `open_mutant_panel`; `selectivity_mutants(st)` → `SelectivityRow`s (P_Ca:P_K measured by Xu vs modelled, wild type first) |
| `ohmic3d.py` | the neutral pore in 3-D: `geometric_conductance(vol)` → `Laplace` (7-point finite volumes, Jacobi-preconditioned CG; g = I/σΔV in m), `conductance_3d(st)` → `Ohmic3D` (Σ σ_s g_s per species in the family's bath; `slice_reading` = lumen-area 1-D), `extrapolate` (linear in h), calibration shapes `cylinder_volume`, `hall_cylinder` |
| `shortfall.py` | Round 7.6: `open_entries` (registered state's first word open/active/activated: 8TKF, 7T3T, 9HEO), `reading(pdb)` → `Reading` (1-D, 3-D at h and 2h, extrapolated, favourable corner; `gain`, `short_by`, `needed_scale`), `open_panel`, `window_scan` (1-D window × S0's margin), `seal_scan`, `grid_scan` |
| `salt_bridges.py` | `salt_bridges(st, cutoff)` → `Bridge`s: Barlow & Thornton ion pairs (charged N–O ≤ `pore_charge.salt_bridge_cutoff`), matched one-to-one closest first over the whole deposit |
| `unitary.py` | `unitary(st, neutralise=)` → `Unitary` (series / neutral / charged / paired = salt bridges cancelled; `n.c.` where unconverged; bath by family, `bath_for`), `published(paralog)`, `unitary_panel(paralog, sweep=)` (the S11 state panel), `sensitivity` (diffusivity × ion-radius corners), `published` (Mak 2000, Vais 2010) |
| `puff_compare.py` | one ruler for all clusters: `MODELS` (the IP3R pair), `ALL_MODELS` (+ `SPARKS`: `SPARK` = `ryr1`, `SPARK_CLEFT` = `ryr1-cleft`, `SPARK_FIT` = `ryr1-cleft-fit`, `fitted_cleft_params`), `spark_scan(model=)`, `spark_ends` (duration, inactivated at start/end, `unterminated`), `MODEL_LABELS`, `params_for` (`mg=`, `k_mg_a=` for a spark receptor; IP3R refuses), `simulate`, `event_sizes`, `recruitment` (Fano, open fraction, blips / multi / large ≥ half the cluster, rate), `coupling_effect(model=)`, `scan_couplings`, `coupling_scan` |

## `ip3r/analysis/` — the findings checks

| File | Key names |
|---|---|
| `checks.py` | framework: `register` decorator, `Check`, `Outcome` (`confirmed/discrepancy/not_run/error`), `run_check(s)`, `all_checks`, `PAPERS`; refuses to confirm against a modified registry |
| `checks_structure.py` | `S0.*` (C4, filter, gate, pore profile, IP3 contacts — recomputed from coordinates) and `P6.shell_agreement`, `P6.contacts_heavy_atom` |
| `checks_constraint.py` | `P5.*` (element means, rankings, gate identity, variant AUCs under S17's position rules, deep-ranks-third, VUS count, ω) and `P6.contacts_vs_core` |
| `checks_evolution.py` | `P2.sister_pair` (own Newick reader), `P2.au_test`, `P2.teleost_itpr1`, `P3.*`, `P4.unreachable`, `LEDGER.claims` |
| `checks_modules.py` | `P6.module_map`, `P6.module_contrast`, `P6.loop_reverses` |
| `checks_shells.py` | `P6.shell_distances` (recomputed), `P6.shell_constraint`, `P6.shell_trend`, `P6.no_contact_step` |
| `fel_rates.py` | S22 §8: S17's FEL sites (`fel_sites` → `Site`) joined to our pocket and modules: `shell_rates(genes)` (omega_by_shell.tsv fields, one-sided rank test vs the protein, BH over the rows), `module_rates(genes)` (`MODULE_PAIRS`, two-sided, BH over nine rows) |
| `checks_rates.py` | `P6.shell_rates`, `P6.module_rates` (rederived) |
| `shell_constraint.py` | `measured_shells` (the 6-deposit pocket), `pocket(gene)` → `Pocket` (carried by own alignment, joined to deep JSD), `shell_rows`, `trend`, `contact_step`, `clear_caches` |
| `module_contrast.py` | `read_alignment`, `reference_columns` (own residue → column map, refused on a sequence mismatch), `tip_identities`, `paired_contrast` → `PairedContrast`, `clear_caches` |
| `stats.py` | `auc` (Mann-Whitney, ties ½), `rank_average`, `mean_by_group`, `sign_test` (exact), `signed_rank_test` (normal approx., tie-corrected), `mann_whitney_greater` (one-sided, tie + continuity corrected), `mann_whitney_less`, `mann_whitney_two_sided` (+ `cles`), `benjamini_hochberg`, `median_interval` (exact order-statistic interval for a median; unbounded when too few), `spearman` (t-distribution p), `fisher_exact` (two-sided), `logistic_fit` (IRLS, Wald p), `wilson` |
| `newick.py` | `parse`, `leaves`, `mrca`, `smallest_clade_containing`, `clade_node` (the node whose leaves are exactly a set), `reroot` (copy rooted above an outgroup or its complement; support labels travel with their edges) |
| `tree.py` | Paper 2's clade questions on `rooted.nwk`: `load_tree`, `group_of` (census prefix), `is_cyclostome`/`genus_of`, `support`, `is_supported` (registered bars), `bipartitions` (root's twin edge counted once), `paralog_clades` → `Clade` (MRCA of named tips; named/unnamed/foreign), `outgroup_clade`, `cyclostome_clades` → `CyclostomeClade` |
| `tree_figure.py` | `layout` (ladderized phylogram), `draw_tree(ax, root, labels, supports)` — clades boxed, cyclostomes marked, supported nodes dotted; `CLADE_COLORS` |
| `tree_robustness.py` | S7 §5.7: `load_pair` (rooted.nwk and the `--bnni` treefile, both RyR-rooted), `largest_pure_clade`, `claim_sets` (the 13 claims by our rules), `compare` → `Claim`s (support in each tree, `verdict`: held / weakened / lost / strengthened / unsupported in both), `strong` |
| `checks_tree.py` | `P2.paralog_clades`, `P2.cyclostome_lineages`, `P2.support_bar`, `P2.bnni_robustness` (rederived from the trees; the Newick travels in the outcome for the exhibit, both for the `--bnni` pair) |
| `genome_grid.py` | Papers 3/4 per cell: `load_grid(layers)` → `GenomeGrid` (309 genomes × `CELLS` ITPR1–3 + RYR; layers `search`/`miss`/`state`/`recovery`/`lesion` (the last from `lesion_strata.layer`); `counts`, `row`, `subset`), `Genome` (N50, level, source, `above_bar`), `recovery_channel(row)` (rebuilt from counts), `above_bar` (registered `genomes.contiguity_bar_bp`), `order`/`ORDERS` |
| `grid_figure.py` | `draw_grid(ax, grid, layer)` (fixed category colours, N50 strip on a fixed log scale, bar line, class blocks), `LAYER_STYLE`, `LAYER_TITLES`; exhibits `draw_misses`, `draw_logistic`, `draw_recovery`, `draw_lesions` |
| `lesion_strata.py` | S15b §8.2–8.3 rebuilt from `integrity_loci.tsv`: `density`, `is_scored` (`lesion.coverage_bar`), `best_loci` (best-covered scored locus per cell), `pairs(loci, matched)` → `Pair` (vs the median of siblings within `lesion.identity_window`; `sign`), `stratify` → `Stratum` (exact sign test per cell × class, BH over strata with ≥ `lesion.min_untied`), `controls` → `Split` (significant strata at the contiguity bar, `siblings`), `layer` (the grid's per-cell sign; `LAYER_VALUES`) |
| `checks_lesions.py` | `P3.lesion_strata` (rederived: pairs, strata, q and the bar split vs `integrity_pairs.tsv`, `lesion_by_class.tsv`, `lesion_class_controls.tsv`) |
| `checks_genomes.py` | `P3.miss_by_contiguity`, `P3.contiguity_tests` (every row of contiguity_tests.tsv), `P4.recovery_channels` (rederived) |
| `range_table.py` | Paper 1's S20/S23 rows: `load_proteomes` → `Proteome` (clade from taxonomy.tsv), `clade_rows` → `CladeRow`, `count_at(rank, name)`, `taxon_calls` (from the 6 assignment tables), `absence_targets` (S23 rule G3, `range.absence_min_proteomes`), `genome_absences` → `GenomeAbsence` (rebuilt from manifest + control ledger + copies + copy ledger; `CONTROLLED`), `copy_numbers`, `substantial_table` (relaxed hits at the registered bar; `LINEAGES`), `load_range` → `Range` (`absences_in(clade)`) |
| `range_genomes.py` | S23 per genome: `genome_rows` → `GenomeRow` (control verdict, copy-ledger status, `copies` from `copies.tsv`, contig N50 vs the genome's own bar; filed under an S20 clade by taxid, else phylum, else class, else `NOT_SWEPT`, `placed_by`), `in_clade`, `clades_with_genomes`, `STATUS_ORDER`, `VERDICT_ORDER` |
| `range_figure.py` | `draw_range(ax, clades, absences)` — clade bars on a fixed 0–1 scale, supergroup colours (`GROUP_COLORS`), prokaryotes collapsed, genome absences crossed; `draw_genomes(ax, rows)` (one clade's S23 genomes: verdict / status / contiguity cells, copies on a fixed 0–`COPIES_MAX` scale; `VERDICT_COLORS`, `STATUS_COLORS`); exhibits `draw_presence`, `draw_relaxed`, `draw_absences`, `draw_copies`, `draw_chase` |
| `family_benchmark.py` | S1's benchmark from the committed sequences: `sequences`, `identity` (full, covered; pairwise Gotoh, memoised on content), `margins` → `Margin` (nearest ITPR vs RyR bait; `call`, `band` at `bench.sister_margin`), `rescore(components, margin)` → `Score` (registered `bench.points_*`; sister cap from our margin, the gate from `FAMILY_SPECIFIC`) |
| `checks_benchmark.py` | `P1.bait_margin` (recomputed from the sequences), `P1.benchmark_counts` (rederived) |
| `checks_range.py` | `P1.presence_range`, `P1.kingdom_absences`, `P1.relaxed_controls`, `P1.absence_targets`, `P1.absences`, `P1.copy_number`, `P1.record_chase` (all rederived) |
| `vus_strata.py` | Paper 5 §8: `stratify(rows, score, gene, layer, sources=)` → `Stratification` (per-position scores per class, both medians, `strata`, `row()` = S17's fields), `stratum_of`, `STRATA`, `CLASS_COLORS`/`STRATUM_COLORS`; `bands(s, level)` → `Bands` (each median's exact order-statistic interval at `vus.median_level`, ± inf = unbounded; `unsure(x)` = the thresholds that could move a VUS, `firm`, `near(s, threshold)`), `NEAR` |
| `vus_figure.py` | `draw_fractions` (the check's exhibit: VUS shares per gene × layer, fixed 0–1), `draw_strip(ax, s, bands=)` (one gene, one layer: classes and medians; with bands, each interval shaded, an unbounded one across the scale, VUS near a median hollow) |
| `checks_variants.py` | `P5.vus_stratification` (every row of vus_stratification.tsv rebuilt; also ClinVar-only), `table_score` |
| `exhibits.py` | `draw(ax, check_id, outcome)` — figures from a check's own numbers |

## `ip3r/render/` (moderngl, OpenGL 4.1)

`camera.py` (`frame(coords, scene=, slab=)`: fit in the current orientation, clip planes from `scene`, optional front clip `slab_front`; `screen_extent` measures the fit), `primitives.py`, `scene.py`, `spline.py`, `geometry_builders.py`,
`shaders/` — ported unchanged from PIEZO1 (impostor spheres/cylinders,
cartoon sweeps, trackball camera). `colormaps.py` — chain, element, fixed
conservation ramp (0.50–0.95 JSD; grey = not scored), fixed displacement ramp
(0–25 Å), `SHELL_COLORS`/`shell_colors` (S22's four shells, grey beyond), `PLDDT_COLORS`/`plddt_colors` (AlphaFold's fixed bands), `SEAM_COLORS`. `representations.py` —
`variant_spheres.py` — `variant_spheres(st, gene, classes, layer, chain_mask)` (Cα of each variant residue on every visible subunit; most decisive class wins; VUS by stratum from the resources), `resource_stratification`, `variant_classes`. `MolecularView` (styles × `ColorBy`, highlight (uniform or per-atom `highlight_rgb`), chain filter, `update_coords`
for animation; `ColorBy.DISPLACEMENT` from a built transition; `ColorBy.LIGAND_SHELL` from `structure.shells`; `ColorBy.PLDDT` for fills only).

## `ip3r/ui/` (PyQt6)

| File | Purpose |
|---|---|
| `app.py` | `main()` — surface format, theme, window, initial load (or `--session FILE`) |
| `main_window.py` | layout and wiring; menus (File: open/save session; View: side/top/IP3 site/fit); `DOCK_WIDTHS` (set on first show), `compact_combos` (wide combos may shrink; they held the Analysis dock at 850 px); loads on workers; `CHECK_SITE_VIEW` (checks whose "Show" centres on an IP3 site); `CHECK_SITES` maps a check to what "Show on structure" highlights, `CHECK_COLOURS` to a colouring (the shell checks), `CHECK_TREE` to the Tree tab, `CHECK_GENOMES` to the Genomes tab and its layer, `CHECK_RANGE` to the Range tab |
| `scene_controller.py` | what the viewport draws: `MolecularView`, pore spheres, site/variant highlights, `show_fill` (the `FillOverlay`), `show_variants` (sphere batch, refused in another numbering), `move_overlays` (spheres follow morph/mode frames), side/top views, mode animation; the camera fit: `fit_target` ("all" = visible subunits, "site" = one IP3 pocket, None once the user navigates), `refit` (on resize and subunit change), `fit_view`, `site_view` (radial, cytosol up, front clipped at the pocket; `SITE_MARGIN`) |
| `fill_overlay.py` | `FillOverlay`: the AlphaFold fill as its own `MolecularView` (`fill:*`, always pLDDT) plus seam bonds (`seams`, red when broken); `move` re-places it on a morph/mode frame |
| `fill_controller.py` | `FillController`: the Completeness selector — builds on a worker, latest request wins, rebuilt on every load, refusal shown in the panel; `fill_html` |
| `gl_widget.py` | `ViewportWidget` (ported; viewport sized from the bound FBO every frame; `navigated` and `resized` signals drive the scene's automatic fit) |
| `structure_panel.py` | deposition list (a tree by family, `FAMILY_LABELS`; RyR1 collapsed; `ids`, `select` expands its group), style, colour, layer, Completeness (+ fill summary and pLDDT/seam legend), subunits, measured sites, legend |
| `channel_panel.py` | `ChannelSummary` text, pore profile vs S0's, state comparison of the loaded family (`set_paralog`: ITPR3 or RYR1), `show_mutants` (RyR1 only), `show_unitary` (conductance per state vs the measured values; `unitary_rows` for the smoke test) |
| `modes_panel.py` | ANM table with irreps and κ (local modes named by residue), animation controls; `select(index, amplitude)` (a session restore) |
| `transition_panel.py` | Transition tab: end state, fit, method, per-family preset (`preset_for`: 8TKG→8TKF, or RyR1's `morph_start`→`morph_end`), frame slider/play (label shows the frame's gate), displacement colouring; plots: the A subspace (headline, always on top) and a `LOWER_PLOTS` selector (gate along the path, every mode by symmetry, element means) |
| `transition_controller.py` | `build_transition` (worker; also the `AtomPath` and `GatePath`), `TransitionController` (install, `coords_at` — matched atoms interpolated, the rest ride their Cα, `show_frame`, `play`, `reset` — path built from the displayed structure) |
| `dynamics_panel.py` | the Dynamics tab: Gating (`gating_panel`), Oscillations (+ window scan), and the Puffs sub-tab; `view_state`/`restore` gather the three (each panel has its own pair) |
| `gating_panel.py` | `GatingPanel` (`MODELS` by key, `select(key)`; redrawn on a parameter edit, or when next shown): DYK, Mak 1998, park/drive (bell + parked fraction), the three side by side at Mak's two IP3 levels (each relative to its own peak), with the flank comparison; or RyR1: Stern scheme vs Murayama bell, plus the scheme fitted to it and that fit under the fibre's Mg²⁺ at both K_Mg,A readings |
| `puffs_panel.py` | `PuffsPanel` (cluster size and coupling `Seeded` per receptor): receptor (DYK / park-drive / RyR1 sparks, mean-field, in the cleft, or in the cleft fitted to Murayama's bell; RyR ignores IP3), coupled vs uncoupled traces, event-size histogram ("no events" when silent), "Scan coupling (both receptors)"; RyR1: free Mg²⁺ and the K_Mg,A reading, "Triggered sparks vs Mg²⁺" (cleft receptors, `spark_mg.mg_scan`); `result` for the smoke test; park/drive also gets the microdomain box (`puffs_domain_view`) |
| `puffs_domain_view.py` | `DomainControls` (store clamp, release multiple, duration = `domain.gui_duration`), `simulate` (worker), `draw` (F/F0 with puffs marked, number open, IPI histogram vs Thurley's Eq. 14 and an exponential of the same mean) |
| `findings_panel.py` | checks by paper, run on a worker, claim/method/verdict, exhibit, show on structure (`showable`: residue-keyed checks drawn on the displayed structure) |
| `tree_panel.py` | Tree tab: `draw_tree` on a toolbar canvas, tip labels / support toggles, "Vertebrates" zoom, click names a tip; "Beside --bnni" (`beside`: the pair and its claims loaded on a worker, `pair`, `claims`, two axes, status = claims held); loaded on a worker when first shown |
| `genomes_panel.py` | Genomes tab: `draw_grid` with layer / sort / class / above-bar controls, click names a genome; loaded on a worker when first shown; `show_layer` (from a check's "Show"; `LAYER_FOCUS` opens the lesion layer on Aves) |
| `range_panel.py` | Range tab: `draw_range` with min-proteomes / collapse-prokaryotes / genome-absence controls, click lists a clade's genome absences; View `CLADES` / `GENOMES` (a clade's S23 genomes, `draw_genomes`; double-click a clade or `show_genomes(clade)`; click names a genome); loaded on a worker when first shown |
| `variants_panel.py` | S17 variants per paralog/class; "Draw on structure" (spheres, `draw_requested`), "VUS by layer" (stratum column marked "near P/LP" / "near B/LB median", `draw_strip` plot with the thresholds' intervals, stratum colours; status counts VUS near each median and names an unbounded one; redrawn on a parameter edit); `follow(paralog)` on load; drawn only in matching numbering |
| `params_dialog.py` | `ParametersDialog`: the registry editor (filter, modified-only, edit with clamp report, reset selected/all, import/export in the `IP3R_PARAMETERS` format; `edit(key, text)` for the smoke test) |
| `params_banner.py` | `ParametersBanner`: the amber "parameters modified" strip, driven by the registry's listeners; hosted in a full-width toolbar (`MainWindow.params_strip`) |
| `session_controller.py` | `SessionController`: File → Save/Open session, `--session`; `capture`, `save_to`, `apply(session, parameters=)` (asks when the saved parameter set differs; applying re-measures), finished from `loaded`, `transition_built` and `modes_computed` in that order (the load, the morph and the ANM run on workers; a frame stops a mode, so the mode restarts last); a restore is dropped if another deposit arrives first |
| `view_state.py` | `set_combo`/`set_spin`/`set_check` (a saved control restored, or a note saying why not), `ParameterFollower` (registry changes as a Qt signal), `Seeded` (spin boxes seeded from parameters follow an edit until typed over) |
| `plot_canvas.py`, `workers.py`, `theme.py` | helpers |

## `ip3r/resources/` (committed)

`parameters.json`, `references.json` (built by `scripts/build_parameters.py`);
`sequences.json`, `domains.json`, `sites.json`, `constraint.json`,
`variants.json`, `structures.json` (built by `scripts/sync_genes.py` from
ip3r_genes, each with the source paths, SHA-256 and ip3r_genes commit);
`ryr1.json` (`scripts/curate_ryr.py`); `ip3r_controls.json` (7T3T, curated by
hand in Round 7.6 from its RCSB entry, hash and reason recorded).

## `scripts/`

| File | Purpose |
|---|---|
| `parameter_table.py` (+ `parameter_table_pd.py`, the park/drive constants; `parameter_table_perm.py`, permeation and wall charge; `parameter_table_graft.py`, AlphaFold fills; `parameter_table_ryr.py`, RyR1 bath, measured conductances, gating and spark constants; `parameter_table_ec.py`, the V channel and the voltage protocol; `parameter_table_domain.py`, the puff microdomain; `parameter_table_pore3d.py`, the voxel grid, seal and Laplace solver; `parameter_table_bench.py`, S1's scorer points, promotion and labelled-bait margin; `parameter_table_views.py`, Round 7.8's lesion scoring and the VUS interval level; `parameter_table_pka.py`, model pKas, the network's electrostatics and sampling, the recording pHs, Xu's P_Ca:P_K), `param_entry.py` (the shared entry constructor), `reference_table.py`, `build_parameters.py` | the registry and its provenance gate (duplicate reference keys fail the build) |
| `sync_genes.py` | import resources from ip3r_genes; `--check` reports drift |
| `curate_ryr.py` | the RyR1 resource from UniProt / InterPro / RCSB: `sequence`, `domains`, `panel` (rules 1–6: `verdict`, `state_of`), provenance hashes; `make ryr` |
| `screenshot_app.py` | scripted GUI smoke test + README screenshots (ends by saving a session, loading elsewhere, restoring, and comparing every field) |
| `screenshot_view.py` | its viewport checks (Round 7.1): molecule reaches the view's edge, viewport share, Structure panel fits its dock, one-subunit refit, IP3 site view, family-grouped list, RyR-only Puffs rows hidden for IP3R |
| `screenshot_ip3r.py` | its Round 7.3 steps: the gating comparison, the A-subspace headline (`check_transition_headline`), the microdomain run; Round 7.7's tree beside `--bnni`; Round 7.8's lesion layer from its check (Aves, 25:2), a clade's S23 genomes, ITPR2's unbounded P/LP interval; Round 7.9's 7LHF fill through the alignment, splice segments named (`IP3R_STEPS`, `ip3r_step`) |
| `screenshot_session.py` | its Round 7.5 steps: panel views set before the session is saved and disturbed after (`set_panel_view`, `disturb_panel_view`), a mode animation saved and restored (`mode_round_trip`), seeded controls following an edit and a typed one not (`check_follow`) |
| `screenshot_sparks.py` | its RyR1 spark steps (`spark_step`, `SPARK_STEPS`): mean-field, cleft, fitted, fitted under 1 mM Mg²⁺ (silent), the triggered Mg²⁺ scan |
| `create_env.sh` | the `ip3r_sim` conda env (PyQt6, moderngl, propka) |

## `tests/`

Camera (`test_camera` — a fit reaches the edge along one axis at any aspect, a narrow view is width-limited, clip planes from the scene not the framed points, the slab follows zoom, 6DQN's pocket is ligand + 15 Å), Morph gate (`test_morph_pore` — both ends atom-for-atom, endpoint frames measure as the deposits for ITPR3 and RyR1, a flipped side chain's chord, rigid side chains caught missing 8TKF's gate), fills (`test_graft` — a rigidly moved chain fills exactly and follows the deposit, a one-off numbering / stubbed flanks / a bent loop / a clashing neighbour each caught, 8TKG's 56 stretches, 9YKK refused, fills beat a straight line on 16 hidden stretches; `test_graft_align` — an isoform with five residues spliced out: the alignment map by hand, the fill lands exactly in deposit numbers, by number it is refused, a stretch needing the spliced residues and one with an insertion refused by name; 7LHF's construct, 7LHF filled from rat isoform 8 with every residue the construct's; 30- and 60-residue windows < 2 Å with seams inside the tolerance; 8TKH 926–943 worse than a line, 7T3P's confident islands all better), transition (`test_transition` synthetic calibrations (the A subspace holds a move spread over two A modes, and is empty with a zero ceiling for a B move); `test_transition_real`
— the drawn end must be 8TKF as a shape, with a case that must fail), physics (`test_anm`, `test_network_checks` — a planted flap after a gap is located and a solid slab has no local modes, 8TKG's #11–15 are the residue-86 flap, the single A mode moves with the cutoff while the collective A modes hold, stride 2 agrees with stride 1 and stride 4 does not, `test_gating`, `test_gating_mak` — the paper's constants, the plateau, a planted K_act dependence the flank test must catch, `test_calcium`, `test_puffs`, `test_park_drive` — stationary = generator null space, printed IP3 functions reproduced, Mak's flank test passed (1.09× / 43×); `test_puffs_pd` — the split step reproduces the clamped stationary P_open to 3 %, and a 5 ms step must fail; `test_puff_compare` — recruitment counted by hand, only park/drive recruits the cluster; `test_puffs_domain` — rest steady under each clamp, the store balance by hand (bound dye counted), one open receptor's closed-form rise and dye equilibrium, Ca²⁺ linear in the number open while dF/F0 compresses past K_d, the cluster samples stationary P_open with every receptor at one Ca²⁺, Thurley's density normalised, the fit recovers a refractory process and does not call an exponential one refractory; `test_permeation` — cylinder and Donnan closed forms, solver = series sum, charge conserved, stubs counted, only 8TKF conducts and falls short of both measurements; `test_selectivity` — GHK round trip, Vais's arithmetic, Planck, TMS at four charges, the Nernst limit, an excluded NMDG⁺'s mouth jump, reversal independent of diffusivity, a charged tract is Ca²⁺-selective and a ring is not, 8TKF short of P_Ca:P_K on every reading; `test_pka` — Henderson–Hasselbalch alone, a fixed neighbour shifts by W/ln10, two coupled acids = their partition function, MC = enumeration on a 12-site double ring at ε 4 (and the ring move is what makes it so), Bjerrum/Debye textbook values; `test_protonation` — Xu's Eq. 1 is GHK without Cl⁻, mean charges conserved on the wall, network radius and PROPKA context wide enough, ring copies agree, lysines charged at any ε, PROPKA buries the E4900 Xu shows is charged, the lysine corners reproduce the acidic reading, no reading of 8TKF reaches 0.1; `test_radial_pb` — the Donnan limit, the Debye–Hückel Bessel cylinder, exact Gauss, grid convergence, a neutral pore blind to the closure, radial between Donnan and uncharged, 8TKF's vestibule screening is not the Ca²⁺ barrier; `test_spline` — frames orthonormal and rotation-minimising (a plane curve keeps its normal); `test_cleft` — release leaves across the edges exactly, grid converged, Eq. 13 by hand, couplings beside Stern's Fig. 9, chessboard neighbours, the Gillespie array samples P_open, cleft sparks end by inactivation; `test_spark_termination` — the fit lands on both flanks and recovers Stern from Stern's bell, rate scaling leaves the steady state alone, an unended spark is flagged, fitted sparks never end and none start at 37 °C, duration grows with Ki; `test_ryr_use` — the state code and masks, the null space against the hand solution, Stern recovered when either gate is removed, the use gate entered only while conducting, a positive cycle flux equal across the cycle's edges (calibrated by a variant that is in detailed balance), Ca²⁺ inactivation relieving the use gate, the bell widened to 2.90 decades, the bell alone not bounding the recovery ratio from below (a fit found down to ρ 0.002, where one start stalled), the fit landing on the measured half-points, only ρ entering the fit (a 7× slower gate at the same ρ fits identically), Fig. 4 read as log₁₀ (τ 1–3 s at +40 mV, zδ 1.14), Fig. 8's residual bound, Ki tracking ρ, and Sitsapesan 1995's −40 mV record admitting the registered rate and rejecting Round 6.9's 0.5 s⁻¹; `test_ryr_mixed` — the mixture by hand, both limits the known schemes, the mask gates entry only, unmasked channels never start used up, the population fit on the measured half-points, Ki climbing as fewer carry, and the decomposition (carriers alone cost nothing, the population fit's gate lengthens sparks >5×); Copello's low-activity bump by hand, its readings ordered, the Ki it forces down, and that it does not rescue termination; `test_use_speed` — Table 1's half times read as half times, the bell moves <20 % at 3500× the speed, the Rios-speed gate ends a triggered spark alone where the bilayer-speed one does not, Rios alone leaves the bell no descending limb, speed rescues the 0.8 mixed cluster, a fast gate suppresses ignition; `test_ryr_two_site` — exits follow the state code, null space = closed form with and without Mg²⁺, the slope ruler on a Hill function, K2/K1 spans slope 1–2, the fit meets both points and the slope (the one-site fit misses the slope), the steeper gate inactivates less at cleft Ca²⁺; `test_mg_competition` — Meissner's Table IV competition predicts Table II's Ka in three salts within 1.6× and no competition misses NaCl 3×, Na⁺ blunts the Mg²⁺ shift, the equivalent reproduces it; `test_spark_mg` — Mg²⁺ 0 leaves the scheme unchanged, closed form = null space under Mg²⁺, Ka moves by the competitive factor, Mg²⁺ inactivates as Ca²⁺ at rest, the trigger opens exactly the available channels, activation-site Mg²⁺ alone shuts every triggered spark uninactivated, the GUI's route and readings build the same scheme; `test_allosteric_v` — null space = Eqs. 6–7, detailed balance, the paper's 0.72 ceiling, fiber 827 gives Stern's plateaus, Stern's Fig. 11 follows the printed rates and Fig. 12 twice them; `test_lumen` — the conversion by hand, the content path's closed forms, a path of ones is the same couplon event for event, an empty store releases nothing, the path is self-consistent, Stern's constants reproduce Fig. 20 (content and corrected plateau), the record grid is fine enough; `test_couplon` — Monte Carlo = master equation through the step and after it, V→C coupling reciprocal, no V current no trigger, Stern's constants reproduce his Fig. 12 with control, the fitted scheme loses control, both-site Mg²⁺ keeps it with no peak; `test_ryr_gating` — null space = product of gates, the printed k_i typo would remove inhibition, coupling by hand, clamped cluster = stationary P_open, step converges and 5 ms is caught, coupling makes sparks; `test_ryr` — the resource's literature anchors (GGGIGD 4894, I4937), each curation rule rejects its own violation, the panel gates at I4937, 9HEO short of 801 pS, D4899Q refutes pairing; `test_salt_bridges` — one Arg cancels one Asp, the cutoff is N–O, a bridged lining group is dropped without adding its partner, 8TKF's D2478–R2471′ bridges), geometry
(`test_symmetry`, `test_pore`, `test_structures_real`), statistics
(`test_stats` — Fisher vs scipy and the tea-tasting value, logistic slope = log OR for a binary predictor, Wilson vs Newcombe, the lower and two-sided Mann-Whitney vs scipy on zero-inflated data, BH by hand; `test_newick` — reroot keeps every split, its support and the tree length, and moves a label to the flipped side of its edge), benchmark (`test_family_benchmark` — both identity metrics by hand, the D7 band, points and both caps, S1's flags ignored), grid (`test_genome_grid` — the channel rule, the bar, ordering, the real grid's counts; `test_lesion_strata` — best-covered locus, the identity window inclusive, the siblings' median, underpowered strata out of BH, the bar split and siblings by hand, both pair tables rebuilt, the layer = the pairs), S23 genomes (`test_range_genomes` — all 194 placed, 13 not swept, copies = the ledger, the own-bar rule, the fixed copy scale), VUS thresholds (`test_vus_bands` — order-statistic ranks by hand, n ≤ 5 unbounded, coverage on a skewed law, a percentile bootstrap's zero width at n = 1, `unsure` at the edges, ITPR2/3's P/LP unbounded), alignment (`test_pairwise` — score equals a cell-by-cell reference DP), shells (`test_shells`), VUS strata (`test_vus_strata` — the rule by hand, ties, a residue in two classes, the resource route = S17's table, one sphere per subunit), tree (`test_tree` — toy trees with known clades, root twin edge, a misplaced tip is foreign), modules (`test_modules` — spans hold their sites, column map lands on the residue, a tampered reference is refused, the literature core = S17's transfer and follows its parameters), provenance (`test_parameters` — listeners fire once per effective change, import is replace-not-merge and refuses a bad file untouched, a memoised measurement made under an edit is dropped on reset,
`test_resources`), sessions (`test_session` — round trip, the field set pinned so a result cannot ride along, malformed files refused by name (a list or dict in a panel view, a bad mode), a file without panel views opens, `replace` semantics; `test_view_state` — a seeded spin follows an edit and a reset, a typed one does not, rounding counted, the getter re-read, a clamp or a refusal noted), rules (`test_sizes`), 3-D pore (`test_ohmic3d` — Hall's cylinder, grid convergence, two pores = 2×, a blind hole changes nothing, a capped axis still conducts sideways, the seal is a drawn pore, one-atom hard sphere exact, the open deposits are the three, 8TKF's seal 20 = 25 Å and 35 Å leaks, 7T3T within a third of 8TKF, the 1-D window moves < 5 %), CLI, and
**`test_checks_calibration.py`** — every check flipped by a planted input
(`PLANTS`), sources proven complete, `not_run` without data, refusal under
modified parameters.
