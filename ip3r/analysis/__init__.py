"""Interpretation: ``checks`` (the findings-check framework) and the check
modules ``checks_structure``, ``checks_constraint``, ``checks_evolution``,
``checks_modules``, ``checks_shells``, ``checks_tree``, ``checks_genomes``, ``checks_range``; ``module_contrast`` (Paper 6's paired
test from the deep alignments); ``shell_constraint`` (conservation against
distance to IP3 over S22's shells);
``stats`` (AUC and friends, stdlib + numpy); ``newick``; ``tree`` (Paper 2's clade questions
asked of ``rooted.nwk``); ``tree_figure`` (the tree drawn);
``genome_grid`` / ``grid_figure`` (Papers 3/4 per genome × cell, and drawn);
``range_table`` / ``range_figure`` (Paper 1 per clade and per genome, and drawn); ``exhibits``
(matplotlib figures that illustrate each finding)."""
